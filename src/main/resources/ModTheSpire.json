{
  "modid": "${ModID}",
  "name": "${project.name}",
  "author_list": [
    "Fujisea, psi_overtake, Tsusena, Venn"
  ],
  "description": "${project.description}",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": [
    "basemod",
    "stslib"
  ]
}

